<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cat.rufull.domain.mapper.lineItem.LineItemMapper">

  <cache/>

  <insert id="insertLineItem" parameterType="com.cat.rufull.domain.model.LineItem">
    INSERT INTO LINE_ITEM(PRODUCT_NAME, PRICE, QUANTITY, ORDER_ID, PRODUCT_ID)
    VALUES(#{productName},#{price},#{quantity},#{orderId},#{productId})
  </insert>
</mapper>